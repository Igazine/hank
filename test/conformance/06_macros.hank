@ "resources/sum"
@ "resources/recursive_a"

() {
  log_print("06 Parse-Time Macros (@)")

  // Shorthand and derivation
  ? math_eq(sum(10, 20), 30) { log_print("Macro inclusion & task derivation: OK") }

  // Recursion
  recursive_a("hello from recursion")
  log_print("Recursive macro expansion: OK")

  // Evaluation result (Void)
  // We test that a task concluding with a macro call returns Void.
  test_void = () {
    @ "resources/sum"
  }
  res = test_void()
  ? !res { log_print("MacroInclude (via Task result) evaluates to Void: OK") }

  log_print("06 Pass")
}
