() {
  log_print("04 Two-Pass Hoisting")

  // Call before definition
  val = hoisted_task(10)
  ? math_eq(val, 20) { log_print("Task hoisting: OK") }

  hoisted_task = (n) {
    ^ math_add(n, n)
  }

  log_print("04 Pass")
}
