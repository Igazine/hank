() {
  log.print("04 Two-Pass Hoisting")

  // Call before definition
  val = hoisted_task(10)
  ? (math.eq(val, 20)) { log.print("Task hoisting: OK") }

  hoisted_task = (n) {
    ^ math.add(n, n)
  }

  log.print("04 Pass")
}
