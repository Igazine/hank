() {
  log_print("22 Instruction Quota")

  // The runner will be configured with a 1000 instruction limit for this file specifically in the test runner.
  
  bomb = () {
    // Just evaluating an empty block repeatedly
  }
  
  // This will throw 4008
  loop_while(1, bomb)
  
  log_print("FAIL: Should not reach here")
}
